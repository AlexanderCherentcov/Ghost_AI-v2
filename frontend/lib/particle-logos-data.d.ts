/** Палитра уникальных цветов [hue, sat, light]. */
export const LOGO_PALETTE: Array<[number, number, number]>;

/** Форма → плоский массив: на каждую точку x, y, z (в тысячных) и индекс цвета. См. lib/particle-logos.ts. */
export const LOGOS_PACKED: Record<string, number[]>;
