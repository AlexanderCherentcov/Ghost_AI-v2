# Fail2Ban filter: ловим 429 Too Many Requests от GhostLine API
# Путь: /etc/fail2ban/filter.d/ghostline-api.conf
[Definition]
failregex = ^<HOST> .* "(GET|POST|PATCH|DELETE) /api/.* HTTP/\d\.\d" 429
ignoreregex =
